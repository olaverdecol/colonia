import React from 'react'
import Layout from '../components/Layout'

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>Página no encontrada</h1>
      <p>La página que ha solicitado no existe.</p>
    </div>
  </Layout>
)

export default NotFoundPage
